# EVPS Environment Configuration Example
# Server Configuration (Handled automatically by hosting platforms like Render)
HOST=0.0.0.0
PORT=8000

# Optional Database Path (Defaults to evps_database.sqlite in project root)
# DATABASE_PATH=/var/data/evps_database.sqlite

# Optional Firebase Admin SDK Configuration
# FIREBASE_PROJECT_ID=evps-phantom-delux
# FIREBASE_CREDENTIALS_JSON={"type": "service_account", "project_id": "...", ...}
